# use_git
for use git 

```
使用git的重点 => (master + dev + feature) 不同 branch(分支)的概念和使用.
  1.项目在每个人自己的feature分支开发,然后合并到dev分支,dev分支所有测试完成,然后合并到master主分支.
  2.每人都基于dev再建自己的分支,在自己的分支上开发完后再合并到dev分支上去.
大家都要遵循git-flow开发规范来使用git.
```
